using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class readme : MonoBehaviour {
    /*
     obaketrue2つ…playerのアタッチ、調整頼む(うまく最初から出てくれないので)
     ms_light のMs_lightControllerのPlayerにはlocalavatarをアタッチ
     (ただ、transform.forwardを用いてRayを生成、あたり判定を行っているスクリプトなので、ライトが当たってゲージがたまるという点ではライトアタッチしてもらった方がよいかも)
     KeyControllerのPlayerにはLocalavaterをアタッチ
     ms_roommanagerのnaviにnaviをアタッチ

     */


	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
